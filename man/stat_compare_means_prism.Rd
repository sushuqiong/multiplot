% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stat_compare_means_prism.R
\name{stat_compare_means_prism}
\alias{stat_compare_means_prism}
\title{Prism-style statistical comparison annotations}
\usage{
stat_compare_means_prism(
  comparisons,
  method = "t.test",
  p.format = c("prism", "signif", "numeric", "scientific"),
  label = c("bracket", "middle"),
  tip.length = 0.02,
  step.increase = 0.12,
  hide.ns = FALSE,
  ...
)
}
\arguments{
\item{comparisons}{A list of length-2 vectors specifying the groups to
compare (e.g. \code{list(c("A","B"), c("B","C"))}).}

\item{method}{Statistical test method. One of \code{"t.test"},
\code{"wilcox.test"}, \code{"anova"}, \code{"kruskal.test"}.
Default \code{"t.test"}.}

\item{p.format}{How to display p-values. \code{"prism"} (default) shows
exact P value or "P < 0.0001"; \code{"signif"} shows significance
stars only; \code{"numeric"} shows raw p-value; \code{"scientific"}
uses scientific notation.}

\item{label}{Position of the p-value label along the bracket.
\code{"bracket"} (default) places the label above the bracket;
\code{"middle"} places it on the bracket.}

\item{tip.length}{Tip length of the comparison brackets in native units
(default 0.02).}

\item{step.increase}{Vertical spacing increment between stacked brackets
(default 0.12).}

\item{hide.ns}{If \code{TRUE}, hide "ns" (non-significant) annotations.
Default \code{FALSE}.}

\item{...}{Other arguments passed to
\code{\link[ggpubr]{stat_compare_means}}.}
}
\value{
A ggplot2 layer (invisibly).
}
\description{
Wraps \code{\link[ggpubr]{stat_compare_means}} with defaults tuned to
match GraphPad Prism's significance bracket formatting: P-value cutoffs
at 0.05 / 0.01 / 0.001 / 0.0001, Prism-style label formatting,
and bracket positioning appropriate for column / bar charts.
}
\examples{
\dontrun{
library(ggplot2)
ggplot(ToothGrowth, aes(supp, len)) +
  geom_boxplot(aes(fill = supp)) +
  ggchoice("prism") +
  stat_compare_means_prism(comparisons = list(c("OJ", "VC")))
}
}
